---
import BaseDocument from '../layouts/BaseDocument.astro';
import { fade } from 'astro:transitions';
import { getPortfolioContent } from '../lib/portfolio';

const { about, site } = await getPortfolioContent();
const pageTitle = `About | ${site.siteTitle}`;
const ogImage = site.ogImage;
const AboutBody = about.Body;
---

<BaseDocument
  title={pageTitle}
  description={about.summary}
  siteName={site.siteTitle}
  ogTitle="About"
  bodyClass="detail-page detail-page--about"
  ogImage={ogImage}
>
  <a
    href="/"
    transition:name="back-nav"
    class="back-link fixed top-7 left-7 z-10 inline-flex items-center gap-2 font-mono text-[0.6rem] tracking-[0.34em] uppercase text-white/35 no-underline transition-colors duration-500 hover:text-white/80"
  >
    <span class="back-arrow"></span>
    back
  </a>

  <main transition:animate={fade({ duration: '0.4s' })} class="mx-auto flex min-h-screen max-w-4xl flex-col justify-center px-5 py-24 md:px-8 md:py-28">
    <header class="mb-12 text-center md:mb-16">
      <h1 class="mb-4 text-4xl font-light tracking-[0.22em] md:text-6xl">About</h1>
      <p class="mx-auto max-w-2xl text-lg leading-relaxed text-balance text-white/70 md:text-2xl md:leading-relaxed">{about.summary}</p>
    </header>

    <section class="grid place-items-center">
      <div class="max-w-2xl text-lg leading-relaxed text-white/60 space-y-6">
        <AboutBody />
      </div>
    </section>
  </main>
</BaseDocument>
